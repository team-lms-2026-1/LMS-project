# --- [1. 공통 설정 (모든 환경 적용)] ---
spring:
  application:
    name: backend
# =====================커뮤니티===============================
    # 파일 업로드 용량 설정 
  servlet:
    multipart:
      max-file-size: 80MB       # 파일 하나당 최대 크기 (기본 1MB)
      max-request-size: 80MB    # 한 번 요청의 전체 크기 (기본 10MB)
# ============================================================
  
  # i18n 메시지 설정
  messages:
    basename: messages/messages
    encoding: UTF-8

  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  
  # Flyway 설정
  flyway:
    enabled: true
    baseline-on-migrate: true
    out-of-order: true # 마이그레이션 순서 유연하게 허용

  # ★★★ [이동됨] AWS S3 설정 (모든 환경에서 사용) ★★★
  cloud:
    aws:
      credentials:
        access-key: ${AWS_ACCESS_KEY} 
        secret-key: ${AWS_SECRET_KEY} 
      region:
        static: ap-northeast-2
      s3:
        bucket: teamlms-bucket # 본인 버킷명 확인 필수

# 🔐 JWT 공통 설정
security:
  jwt:
    secret: ${JWT_SECRET:THIS_IS_A_LONG_SECRET_KEY_FOR_JWT_32_CHARS_MIN}
    access-token-seconds: 3600
    
logging:
  level:
    org.springframework.security: DEBUG

---
# --- [2. 로컬 개발 환경 (내 컴퓨터)] ---
spring:
  config:
    activate:
      on-profile: local
  
  datasource:
    url: jdbc:postgresql://localhost:5432/team_lms
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate 
    show-sql: true

---
# --- [3. 배포 환경 (EC2 + RDS)] ---
spring:
  config:
    activate:
      on-profile: prod
  
  datasource:
    url: jdbc:postgresql://${RDS_ENDPOINT}:5432/postgres
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

# [기타 설정]
api:
worknet:
  key: "h3Fc15RT7IE4WhljLA0kr4FiYG7SBGdR"
  base-url: "http://openapi.work.go.kr/opi"